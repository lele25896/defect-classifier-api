__pycache__/
*.pyc
.pytest_cache/

# MVTec AD is license-gated — don't commit the dataset
data/downloads/
data/*/

# trained checkpoints (bottle/hazelnut, ~45MB each) are committed — the
# Docker build needs them locally, git-ignoring them left CI's built image
# with zero models loaded ("categories": [] in /health, /predict always 404)

terraform/.terraform/
terraform/*.tfstate*
!terraform/terraform.tfvars
