# torch/torchvision NOT listed here — installed separately as CPU-only
# wheels (see Dockerfile / CI). Default PyPI torch pulls the CUDA build
# (~3GB with nvidia-* deps) even for inference-only CPU serving.
fastapi==0.115.5
uvicorn==0.32.1
python-multipart==0.0.17
pillow==11.0.0
numpy==2.1.3
